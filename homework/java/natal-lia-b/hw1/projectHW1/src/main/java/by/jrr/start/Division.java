package by.jrr.start;

class Division {
    Division(int a, int b) {
        System.out.println("a / b = " + (a / b));
    }

    Division(double a, double b) {
        System.out.println("a / b = " + (a / b));
    }
}
